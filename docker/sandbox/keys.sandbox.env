# Dummy sandbox env — fake values so the image never carries real secrets.
# Real LLM/payment keys come from the host env at runtime, NEVER from the repo.
# When Backend Core lands, add its required keys here with fake values.
NODE_ENV=test
OPENAI_API_KEY=sk-sandbox-fake
ANTHROPIC_API_KEY=sk-ant-sandbox-fake
PAYMENT_API_KEY=sandbox-fake
PRICE_BRL=0
